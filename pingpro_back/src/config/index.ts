/**
 * Barrel de configuración: permite `import { database, db } from '@config/index'`
 * en lugar de importar archivo por archivo.
 */
// Config barrel
export * from './firebase';
export * from './database';
